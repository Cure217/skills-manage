interface:
  display_name: "对话 HTML 总结"
  short_description: "自动提炼当前对话重点，生成结构清晰、美观且可自动打开的 HTML 总结页。"
  default_prompt: "使用 $conversation-html-summary 总结今天所有 Codex 会话，生成包含结论、时间线、文件线索和下一步的 HTML 日报页，并在生成后自动打开该 HTML。"

policy:
  allow_implicit_invocation: true
